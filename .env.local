# Local overrides – KHÔNG commit file này
# Production: https://disasterrelief-api.runasp.net
# Local BE:   http://localhost:5092  (xem launchSettings.json của BE)
VITE_API_BASE_URL=https://disasterrelief-api.runasp.net/api
VITE_APP_TITLE=ReliefConnect

# true  → dùng mock data (test không cần backend)
# false → gọi real backend (đặt false khi đã có token thật)
VITE_USE_MOCK_AUTH=true
