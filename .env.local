# Local overrides – KHÔNG commit file này
# Local backend: http://localhost:5001
# Production:    https://<ten-backend>.onrender.com
VITE_API_BASE_URL=http://localhost:5001/api
VITE_APP_TITLE=ReliefConnect

# true  → dùng mock data (để test, không cần backend)
# false → gọi real backend (đặt false khi connect BE)
VITE_USE_MOCK_AUTH=true
